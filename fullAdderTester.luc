module fullAdderTester (
    // Mojo outputs A,B,Cin, then checks for correct inputs S and Cout. (With reference to itself)
    output a,
    output b,
    output cin,
    input sum,
    input cout
) {
  
  always {
    
      a = io_dip[2][0];
    
      //correctSum = (a[0] ^ a[1]) ^ (a[2]);
      //correctCout = (((a[0] ^ a[1]) & a[2]) | (a[0] & a[1]));
  
    }
  }


}